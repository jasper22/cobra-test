{
    "tasks": [
        {
            "label": "go: build (debug)",
            "type": "shell",
            "command": "go",
            "args": [
                "build",
                "-gcflags=all=-N -l",
                "-o",
                "${workspaceFolder}\\app.exe"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
        }
    ]
}